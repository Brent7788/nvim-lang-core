local MainFoldr = {
    foldr_path = 'defaullt'
}

function MainFoldr.generte_foldr(systim, procces)
    if procces == 0 then

    end

    return {
        foldr_path = systim
    }
end

return MainFoldr
